import React, {Component} from "react";

export default class DinnerMenu extends Component {
    constructor(props) {
        super(props)
        }
    render() {
        return <p>Dinner on (date goes here)</p>
    }
}