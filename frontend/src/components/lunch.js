import React, {Component} from "react";

export default class LunchMenu extends Component {
    constructor(props) {
        super(props)
        }
    render() {
        return <p>Lunch on (date goes here)</p>
    }
}